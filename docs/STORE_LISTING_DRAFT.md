# QPdf store listing draft — superseded

The listing copy that actually ships lives in
[`STORE_SUBMISSION.md`](STORE_SUBMISSION.md) §8, and that is the single source of
truth for both stores.

This file used to hold a second, independently edited copy of the same text. It
drifted: it still advertised "fill and sign interactive forms" and "form
authoring" after AcroForm filling was removed in `refactor(fill)`, which is the
kind of inaccurate-metadata claim that gets a submission rejected. Rather than
maintain the same paragraphs in two places, the copy has been removed and this
file kept only as a signpost.

Edit §8. Nothing reads this file.
