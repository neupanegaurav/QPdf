# pdf_engine_api

Internal QPdf contract for opening documents through a replaceable PDF engine.
Application code depends on this interface instead of a specific parser or
native library.

This package is not published independently.
