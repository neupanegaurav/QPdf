# pdf_domain

Internal QPdf package containing engine-neutral document models, errors, and
bounded undo/redo command history. It has no PDF renderer dependency, keeping
the product layer portable across Flutter targets.

This package is not published independently.
